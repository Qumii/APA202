@{
    int count = 0;
}


<div class="row">

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Sliders</h4>
                <a asp-action="create" class="btn btn-outline-success btn-fw">Create Slider</a>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>
                                    №
                                </th>
                                <th>
                                    Image
                                </th>
                                <th>
                                    Title
                                </th>
                                <th>
                                    Subtitle
                                </th>
                                <th>
                                    Order
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (Category category in Model)
                            {
                                <tr>
                                    <td>
                                        <p> @(++count)</p>
                                    </td>
                                   
                                    <td>
                                        @category.Name
                                    </td>
                                    <td>
                                        @category.Products.Count
                                    </td>
                                   
                                    <td>
                                        <a asp-action="update" asp-route-id="@category.Id" class="btn btn-outline-warning btn-fw">Edit</a>
                                        <a asp-action="detail" asp-route-id="@category.Id" class="btn btn-outline-info btn-fw">Detail</a>
                                        <a asp-action="delete" asp-route-id="@category.Id" class="btn btn-outline-danger btn-fw">Delete</a>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
